# webdamga çalışma zamanı bağımlılıkları
# Kurulum: pip install -r requirements.txt && python -m playwright install chromium
playwright>=1.47
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
typer>=0.12
rich>=13.7
