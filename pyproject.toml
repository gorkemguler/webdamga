[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "webdamga"
version = "0.1.0"
description = "Local web evidence tool: captures a URL with screenshots, raw bytes, a replayable WARC, a signed SHA-256 manifest and trusted timestamps, and watches it for changes."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
keywords = ["web-archiving", "forensics", "screenshot", "phishing", "evidence", "playwright", "urlscan", "har", "mhtml", "warc", "minisign", "rfc3161", "opentimestamps"]
authors = [{ name = "webdamga contributors" }]
dependencies = [
    "playwright>=1.47",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "jinja2>=3.1",
    "python-multipart>=0.0.9",
    "typer>=0.12",
    "rich>=13.7",
    "cryptography>=42",
    "Pillow>=10.4",
]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.6", "httpx>=0.27", "warcio>=1.7"]

[project.scripts]
webdamga = "webdamga.cli:main"

[project.urls]
Homepage = "https://github.com/gorkemguler/webdamga"
Issues = "https://github.com/gorkemguler/webdamga/issues"

[tool.hatch.build.targets.wheel]
packages = ["webdamga"]

[tool.hatch.build.targets.wheel.force-include]
"webdamga/web" = "webdamga/web"

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
